gostatus - AGPLv3 - A microservices monitoring web application.

=== ChangeLog ===
    (8 items par revision)
    Update version and revision in src/main.go's getIntroText()
    Add a git tag for r-1 then push --tags for each revision
    SLOC is the result of `sloc src/ static/js/`

* v0.0.1-16 (31 Dec. 2024 - ???) SLOC ???
- (7) Remove referred Client.disconnect to fix actual error
- First mongo InsertOne call after a db type cast
- Add table style to improve its readability
- Add and use the full-width-row CSS rule on all pages
- Fix second row width issue on all templates
- Fix sidebar width issue on large screens
- Add Service type with bson attributes
