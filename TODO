gostatus - AGPLv3 - A microservices monitoring web application.

=== ChangeLog ===
    (8 items par revision)
    Update version and revision in main.go's getIntroText()
    Add a git tag for r-1 then push --tags for each revision
    SLOC is the result of `sloc .`

* v0.0.0-3 (26 Jan. 2023 - ???) SLOC ???
- (5) Close the webcontent handling ROADMAP section
- Make the hello page unit test compile correctly
- Now serve static files (and add body margin)
- Correctly handle header template in content/partials
- Add main's getTemplates() implementation
